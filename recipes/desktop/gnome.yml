modules:
  - type: dnf
    install:
      install-weak-deps: false
      exclude:
        - NetworkManager
      packages:
        - gnome-shell
        - gnome-console
        - nautilus
        - xdg-user-dirs
        - xdg-user-dirs-gtk
        - flatpak
    group-install:
      packages:
        - fonts